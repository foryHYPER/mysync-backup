/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  experimental: {
    turbo: {
      rules: {
        // Turbopack rules configuration
      },
    },
  },
};

module.exports = nextConfig; 